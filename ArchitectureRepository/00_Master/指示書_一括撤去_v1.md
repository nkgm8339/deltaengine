# 指示書_一括撤去_v1

## Order Flow Analysis Platform / DeltaEngine — Codex 実装用

> 母体仕様: `仕様書_5指標独立化_v3.1（確定版）` §9 step3。本書は5指標全独立完了後の**畳み装置一括撤去**を指示する。全5指標（CVD/Footprint/Imbalance/Absorption/Flow）が独立済みであることが前提。
> 実装体制: Codex。根拠はすべて実物（Flow 独立化反映後の最新コード）の file:行。本書に無いことを足さない。

---

## 0. Codex への絶対命令

1. 確認や承認を求めず、全手順を即座に完全実行せよ。
2. 本書のパッチは Flow 独立化完了後の実物を前提にしている。**貼る前に該当箇所を grep で再確認し、周辺が一致することを検めてから**当てよ。
3. 5指標の独立化済み新経路（CVD 傾き・Footprint VA 歯車・Imbalance 壁・Absorption パネル・Flow 個別イベント）を**絶対に壊すな**。
4. `float(` を新規コードに書くな。
5. 完了後、§8 の完了条件を満たして報告せよ。

---

## 1. スコープ（一括撤去）

仕様 §9 step3: 全5指標が独立し composite の entries が空になった。ここで初めて畳み装置の本体を撤去する。

**撤去対象（仕様 §4 全項）**:
1. composite / direction / signal 枠 / confidence（`signal.py evaluate`）
2. market_state / risk_level / summary（`analysis.py AnalysisEngine.evaluate`）
3. confluence（`push_broker.py confluence()`）
4. SimpleAverageFlowScorer / score_flow_events / FlowScorer Protocol / w_flow（`signal.py`）
5. score_cvd / score_footprint / score_imbalance（`signal.py`）
6. apply_trend_filter / EmaTrendDetector / TrendState / REASON_TREND_FILTER（`trend.py` ファイル削除）
7. 配信 payload の旧フィールド（signal/confidence/composite/confluence/scores/market_state/risk_level/veto/cvd_unref）
8. PushFlowEvent / _imbalance_should_fire 関連の dead code
9. push_broker の flow_score / _FlowRecord / _flow_history / confluence メソッド
10. UI: 旧スコアパネル / MARKET STATE パネル / confluence パネル / risk/veto/rr 表示

---

## 2. 撤去手順

### Phase A — Python バックエンド

#### A-1. `src/orderflow/signal.py` の大幅縮小

**撤去するもの（上から順に削除）**:

- L63-100: `FlowScorer` Protocol / `SimpleAverageFlowScorer` クラス / `_DEFAULT_FLOW_SCORER`
- L103-113: `score_flow_events` 関数
- L271-314: `score_cvd` / `score_footprint` / `score_imbalance` 関数
- L258-268: `_clamp` / `_CLAMP_MAX` / `_CLAMP_MIN`

**`SignalEngine.evaluate` の書き換え**:

全5指標が独立した後、`evaluate` は呼ばれても entries 空 → NO_INPUT → WAIT を返すだけ。本体を **stub 化**する。

現行の `evaluate`（L167-255）を以下に置換:

```python
    def evaluate(
        self,
        cvd_score: Optional[Decimal] = None,
        fp_score: Optional[Decimal] = None,
        imb_score: Optional[Decimal] = None,
        absorption_result: Optional[Any] = None,
        flow_events: Optional[Any] = None,
    ) -> SignalResult:
        """All 5 indicators are independent; composite is retired.

        Returns a fixed NO_INPUT WAIT result for backward compat with storage
        and any code that still calls evaluate. Will be fully removed when the
        storage schema migrates away from signal_to_row.
        """
        return SignalResult(
            composite=_ZERO,
            confidence=_ZERO,
            signal="WAIT",
            reasons=(REASON_NO_INPUT,),
            flow_score=None,
        )
```

**`SignalEngine.__init__` の縮小**: 重み・threshold 引数は残すが使わない（呼び出し側の署名互換）。ただし `from_config` のパース箇所が壊れないよう `__init__` は**そのまま残す**。

**`SignalResult` は残す**（storage.add_signal / signal_to_row が参照するため）。

**import 整理**: `ImbalanceResult` / `FlowEvent` の import は撤去対象の関数が消えるため不要化。削除する。`AbsorptionResult` import も veto 撤去済みで不要。削除する。残すのは `_to_decimal`（`from_config` 内で使用）。

**REASON 定数**: `REASON_NO_INPUT` は残す。他の REASON 定数（`REASON_CVD_UP` 等）は evaluate 内で生成しなくなるが、テストやログで参照されうるため**残置**（dead code 化のみ。lint 警告は無視可）。

#### A-2. `src/ai/analysis.py` の stub 化

`AnalysisEngine.evaluate` は signal/confidence に依存。全 composite が死んだため、固定値を返す stub にする。

現行の `evaluate`（L59-106）を以下に置換:

```python
    def evaluate(self, inp: AnalysisInput) -> AnalysisResult:
        """All 5 indicators are independent; market_state classification is retired.

        Returns a fixed NEUTRAL / LOW result for backward compat.
        """
        return AnalysisResult(
            analysis_time=inp.analysis_time,
            symbol=inp.symbol,
            market_state="NEUTRAL",
            confidence=_ZERO,
            risk_level="LOW",
            summary="Independent indicators active",
            reasons=tuple(inp.signal_result.reasons) if inp.signal_result else (),
        )
```

`AnalysisInput` / `AnalysisResult` dataclass は**残す**（pipeline / push_broker / mt5 が参照）。

#### A-3. `src/orderflow/trend.py` をファイル削除

仕様 §6.4: 消費先は `apply_trend_filter` 1箇所のみ（全数 grep 確認済み）。ファイルごと削除。

#### A-4. `src/pipeline.py` の撤去

**(4-a) import 整理**:

削除する import 行:
```python
from .orderflow.divergence import CvdDivergenceDetector
from .orderflow.trend import EmaTrendDetector, apply_trend_filter
```
→ `CvdDivergenceDetector` は divergence 配線（CVD 独立化で残した）で**まだ使用中**。**削除しない。**
→ `EmaTrendDetector, apply_trend_filter` のみ削除。

```python
from .orderflow.signal import SignalEngine, SignalResult, score_cvd, score_footprint, score_imbalance
```
→ `score_cvd, score_footprint, score_imbalance` を削除（signal.py から消えたため）:
```python
from .orderflow.signal import SignalEngine, SignalResult
```

**(4-b) `_evaluate_and_store` の撤去箇所**:

1. `s_cvd = None` のコメントブロック（L504-507）→ 行ごと残置で可（None 代入は副作用なし）。ただし `score_cvd` / `score_footprint` / `score_imbalance` の参照がなくなるため、残っている場合は削除。

2. `apply_trend_filter` 呼び出し（`pipeline.py:526`）を削除:
   ```python
       signal_result = apply_trend_filter(signal_result, trend_state)
   ```
   → 行ごと削除。

3. `trend_state` 引数（`_evaluate_and_store` L485）は使わなくなる。**引数は残す**（呼び出し側の署名互換。次の掃除で除去可）。

**(4-c) ReplayPipeline / LivePipeline の trend 配線を削除（両系）**:

ReplayPipeline:
- L342: `trend_detector = EmaTrendDetector()` → 行削除
- L391: `self.trend_state = trend_detector.update(closed_higher["15m"])` → 行削除（if "15m" ブロック自体は higher_timeframes 用途で残す。trend_detector 行のみ削除）

LivePipeline:
- L854 付近: `trend_detector = EmaTrendDetector()` → 行削除（grep で正確な行を特定）
- L951: `self.trend_state = trend_detector.update(closed_higher["15m"])` → 行削除

**(4-d) PushFlowEvent**:

`PushFlowEvent` dataclass（`pipeline.py:172-181`）と `_IMBALANCE_COOLDOWN_BARS`（L79）は Absorption の PushFlowEvent 配信（`pipeline.py:955-967`）でまだ使用中。**削除しない。** `_imbalance_should_fire` / `imbalance_fire_state` 関連は Imbalance 独立化の手順2で IMBALANCE PushFlowEvent ブロックを削除済みのため dead code 化しているが、本書では**残置**（掃除は別途）。

#### A-5. `webapp/push_broker.py` の撤去

**(5-a) `confluence` メソッド（L150-188）を丸ごと削除。**

**(5-b) `flow_score` メソッド（L133-148）を丸ごと削除。**

**(5-c) `_FlowRecord` dataclass（L84-86）を削除。** `__init__` の `self._flow_history` 初期化行（L106）も削除。

**(5-d) `__init__` の引数から confluence 関連を削除:**
- `confluence_score_threshold` / `confluence_strength_threshold` 引数と `self.conf_score_th` / `self.conf_strength_th` 代入を削除。
- `flow_window_sec` 引数と `self.flow_window_sec` 代入を削除。

**(5-e) `on_analysis` の payload から旧フィールドを削除:**

ANALYSIS envelope dict（L241-292）から以下のキーを削除:
- `"signal"`: signal_result.signal
- `"confidence"`: d2s(signal_result.confidence)
- `"composite"`: d2s(getattr(signal_result, "composite", None))
- `"scores"`: {k: d2s(v) for k, v in scores.items()}
- `"cvd_unref"`: module_scores.get("cvd") is None
- `"confluence"`: self.confluence(...)
- `"market_state"`: analysis_result.market_state
- `"risk_level"`: analysis_result.risk_level
- `"veto"`: veto
- `"reasons"`: list(analysis_result.reasons)
- `"expected_rr"`: None

`scores` ローカル変数（L230-235）と `veto` ローカル変数（L236-239）も不要化。削除。

**残すキー**: `"divergence"` / `"imbalance"` / `"absorption"` / `"flow_events"`（5指標独立化で追加したもの）。

結果、`on_analysis` の envelope は以下の形になる:

```python
    async def on_analysis(self, analysis_result, signal_result, module_scores, absorption_result, divergence=None, imbalance_result=None, imbalance_detector=None, flow_events=None) -> None:
        now = analysis_result.analysis_time
        await self._broadcast(envelope("ANALYSIS", now, self.symbol, {
            "divergence": None if divergence is None else {
                ... (既存のまま)
            },
            "imbalance": None if imbalance_result is None else {
                ... (既存のまま)
            },
            "absorption": None if absorption_result is None else {
                ... (既存のまま)
            },
            "flow_events": None if not flow_events else [
                ... (既存のまま)
            ],
        }))
```

引数 `signal_result` / `module_scores` は呼び出し側（`main.py`）との署名互換で**残す**（中で使わない）。

#### A-6. `webapp/main.py`

- `on_analysis_cb` 内で `broker.on_analysis` に渡す引数は変更なし（署名互換）。
- absorption POST エンドポイント（L339-）は無変更。
- `from decimal import Decimal` 等の import は既存のまま。

#### A-7. `src/mt5/adapter.py`

`analysis_to_mt5_message`（L245-258）は `AnalysisResult` の `market_state` / `confidence` / `risk_level` / `summary` / `reasons` を読む。stub 化後は固定値（NEUTRAL / 0 / LOW / ...）が送出される。**MT5 側が固定値を受けて問題ないか**はお館様判断だが、本書時点では `analysis_to_mt5_message` は**無変更**とする（MT5 は `enabled: false` が既定。壊れる実害なし）。

---

### Phase B — クライアント（`webapp/static/index.html`）

#### B-1. MARKET STATE パネルを撤去

MARKET STATE パネルの HTML（`#right` 内の該当パネル）を削除。`index.html` で `mschip` / `MARKET STATE` を grep して位置を特定し、パネル div ごと削除。

#### B-2. SIGNAL — WHY パネルの旧フィールド撤去

`renderAnalysis()` 内（`index.html:865-912`）から以下を削除:
- `mschip` の表示（market_state）
- `confstars` / `confgrid`（confluence 表示）
- `whysig` / `whyconf`（signal / confidence 表示）
- `vetobadge`（veto 表示）
- `scoreRow("COMPOSITE", ...)` 行（score パネル）
- `risk` / `rr` / `veto` 表示行
- `$("scores").innerHTML = ...` ブロック全体

`renderAnalysis()` は以下だけ残す（5指標の独立所見表示）:
```javascript
function renderAnalysis(){
  const a=S.analysis; if(!a)return;
  renderCvdDivergence(a.divergence);
  if(a.imbalance){ ... }  // 既存のまま
  renderImbalance();
  ABS=a.absorption||null;
  renderAbsorption();
}
```

#### B-3. 旧 HTML 要素の削除

以下の HTML 要素を削除（パネル内の div/span。grep で正確な位置を特定）:
- `#mschip` を含む MARKET STATE パネル全体
- `#confstars` / `#confgrid` を含む CONFLUENCE セクション
- `#whysig` / `#whyconf` / `#vetobadge` を含む SIGNAL — WHY セクション
- `#scores` を含むスコアパネル
- `#risk` / `#rr` / `#veto` 表示

**注意**: `renderTopSignal()`（topbar のシグナル表示）も signal に依存しうる。grep で確認し、旧 signal 参照があれば撤去。

#### B-4. `scoreRow` 関数の削除

`scoreRow`（`index.html:855-863`）は全指標がスコアパネルから消えたため呼び出し元がなくなる。関数定義を削除。

#### B-5. `catColor` / `sigColor` の整理

`sigColor` が signal 文字列（BUY/SELL/WAIT）を色に変換する関数なら、signal 撤去後は不要化。grep で消費先を確認し、不要なら削除。`catColor` は FLOW EVENTS パネルで使用中のため**残す**。

---

## 3. テスト（仕様 §10 失効方針）

**大量のテストが失効する。方針: 撤去対象の振る舞いを検証していたテストは削除。テストを緑にするために撤去対象を延命しない。**

### 削除対象テスト（ファイル単位で判断）

| ファイル | 判断 |
|---|---|
| `tests/orderflow/test_signal.py` | composite / signal 枠のテスト。**全件が撤去対象の検証** → **ファイル削除** |
| `tests/orderflow/test_signal_flow.py` | SimpleAverageFlowScorer / flow→composite のテスト → **ファイル削除** |
| `tests/orderflow/test_signal_normalization.py` | score_cvd / score_footprint / score_imbalance のテスト → **ファイル削除** |
| `tests/orderflow/test_trend.py` | EmaTrendDetector / apply_trend_filter のテスト → **ファイル削除** |
| `tests/ai/test_analysis.py` | market_state / risk_level 写像のテスト → **ファイル削除** |

### 更新対象テスト（部分修正）

| ファイル | 修正内容 |
|---|---|
| `tests/test_pipeline.py` | signal_result が WAIT/NO_INPUT になることを前提に期待値更新 |
| `tests/test_live_pipeline.py` | signal_result.signal が WAIT、flow_score が None であることを前提に更新 |
| `tests/test_pipeline_absorption.py` | veto 関連のアサーションを削除（veto は既に signal.py から撤去済み。本書で残存テストを掃除） |
| `tests/webapp/test_push_broker.py` | confluence / flow_score / scores dict のテストを削除。on_analysis payload の新形状（divergence/imbalance/absorption/flow_events のみ）に更新 |

### 新規テスト

- `signal.py` stub: `evaluate` が常に NO_INPUT WAIT を返すことの1本。
- `analysis.py` stub: `evaluate` が常に NEUTRAL/LOW を返すことの1本。
- ANALYSIS payload の新契約: divergence / imbalance / absorption / flow_events の4キーのみ存在することの1本。

---

## 4. storage 保存スキーマ

`signal_to_row`（`schema.py:107-117`）は `signal_result.signal` / `signal_result.confidence` を保存する。stub 化後は常に `WAIT` / `0` が書き込まれる。

**本書時点では schema を変更しない。** `signal_to_row` / `storage.add_signal` はそのまま残す（WAIT/0 が保存される）。新5指標所見を保存する新スキーマへの移行は**別作業**とする。

---

## 5. config.yaml の整理

以下の config キーは消費先が消える:
- `signal.weight.cvd` / `signal.weight.footprint` / `signal.weight.imbalance` / `signal.weight.flow`
- `signal.confidence_threshold`
- `signal.absorption_veto_threshold`
- `signal.cvd_slope_ref`
- `signal.stack_ref`

**本書時点では config キーを削除しない。** `SignalEngine.__init__` / `from_config` がこれらを読み込むが、`evaluate` 内で使わないため実害なし。config キーの削除は `from_config` / `LivePipeline.from_config` の引数掃除と併せて行う（別作業）。

---

## 6. ファイル変更サマリ

| ファイル | 操作 |
|---|---|
| `src/orderflow/signal.py` | evaluate stub 化、score_* / FlowScorer / SimpleAverageFlowScorer / _clamp 削除、import 整理 |
| `src/ai/analysis.py` | evaluate stub 化 |
| `src/orderflow/trend.py` | **ファイル削除** |
| `src/pipeline.py` | import 整理、apply_trend_filter 呼び出し削除、trend_detector 生成・update 削除（Replay/Live 両系） |
| `webapp/push_broker.py` | confluence / flow_score / _FlowRecord 削除、on_analysis payload 縮小、__init__ 引数整理 |
| `webapp/static/index.html` | MARKET STATE / CONFLUENCE / SIGNAL-WHY / scoreRow / scores パネル / risk/rr/veto 撤去、renderAnalysis 縮小 |
| テスト5ファイル | ファイル削除（test_signal / test_signal_flow / test_signal_normalization / test_trend / test_analysis） |
| テスト4ファイル | 部分更新（test_pipeline / test_live_pipeline / test_pipeline_absorption / test_push_broker） |
| テスト新規 | stub 検証 + 新 payload 契約（3本程度） |

---

## 7. 実行順序

1. Phase A（Python）を A-1 → A-2 → A-3 → A-4 → A-5 → A-6 → A-7 の順で実行。
2. Phase B（HTML）を B-1 → B-2 → B-3 → B-4 → B-5 の順で実行。
3. テスト: §3 の削除 → 更新 → 新規の順で実行。
4. `python -m pytest -q` で全件 green を確認。

---

## 8. 検証・完了条件

1. `python -m pytest -q` 全件 green（実数併記。大幅減少が想定される）。
2. `grep -rn "float(" src/orderflow/signal.py src/ai/analysis.py src/pipeline.py webapp/push_broker.py webapp/main.py` で新規 float 追加ゼロ。
3. `src/orderflow/trend.py` が存在しないこと。
4. HTML 機械検証:
   - `grep -c 'scoreRow' webapp/static/index.html` → **0**
   - `grep -c 'market_state' webapp/static/index.html` → **0**（JS 内の参照がすべて消えたこと）
   - `grep -c 'confluence' webapp/static/index.html` → **0**
   - `grep -c 'mschip' webapp/static/index.html` → **0**
5. grep で 5指標の独立化済み要素が残存していることを確認:
   - `grep -c 'renderCvdDivergence' webapp/static/index.html` → 1 以上
   - `grep -c 'computeValueArea' webapp/static/index.html` → 1 以上
   - `grep -c 'computeWalls' webapp/static/index.html` → 1 以上
   - `grep -c 'renderAbsorption' webapp/static/index.html` → 1 以上
   - `grep -c 'flow_events' webapp/static/index.html` → 1 以上
6. `CHANGELOG.md` に新エントリ。`CompletionLog.md` に実施記録。

**完了3点セット**: (1) CompletionLog 追記 (2) `DeltaEngine_一括撤去_完了.zip`（全体） (3) チャット報告と ZIP の両方提出。

---

## 9. 触ってはいけないもの

- CVD 傾き計算（`index.html` の `cvdSlope()` / CVD 歯車）
- Footprint VA 再計算（`computeValueArea` / FP 歯車）
- Imbalance 壁検出（`computeWalls` / IMB 歯車 / サーバ ANALYSIS payload の imbalance ブロック）
- Absorption パネル（`renderAbsorption` / サーバ ANALYSIS payload の absorption ブロック / POST endpoint）
- Flow 個別イベント（ANALYSIS payload の flow_events / FLOW WS メッセージ / FLOW EVENTS パネル）
- divergence（`CvdDivergenceDetector` / pipeline の divergence 配線 / ANALYSIS payload の divergence ブロック）
- `flow_detector.py`（5検出器本体）
- `absorption.py`（AbsorptionDetector 本体 / set_params）
- `imbalance.py`（ImbalanceDetector 本体 / last_effective_min_volume）
- storage スキーマ（`signal_to_row` / `candle_to_row` / `trade_to_row`）
- FLOW EVENTS パネル（`#flowpanel` / `renderFlow` / `onFlow`）
- ABSORPTION PushFlowEvent 配信（`pipeline.py:955-967`）

---

## 10. 本書対象外（後続作業候補）

- storage 新スキーマ（5指標所見の永続化）
- config.yaml の不要キー掃除（signal.weight.* / confidence_threshold 等）
- `SignalEngine` / `AnalysisEngine` の完全削除（stub ではなくファイル削除。呼び出し側の掃除が先）
- `PushFlowEvent` / `_imbalance_should_fire` / `imbalance_fire_state` 関連の dead code 掃除
- MT5 adapter の payload 更新（独立指標所見を送出）
- 歯車の Flow 版追加
- 5指標の最終画面配置
