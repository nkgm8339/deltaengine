# Claude Code 指示書：Codex向け引き継ぎ書の作成（最終作業）

作成日: 2026-07-27 JST
対象プロジェクト: `Delta_Engine_Pro4web`（branch: ui-refresh-v2）
前提: Ingestion Adapter骨格 完了承認済み（コミット e001031）

## ■ 背景

本作業をもって、Strategy Engine関連の実装作業はClaude CodeからCodexへ移行する。Claude Codeが実施した4工程（Replay契約拒否試験 / Engine本体骨格 / 実Predicate Evaluator骨格 / Ingestion Adapter骨格）の全体像・設計判断・残課題を、実装した本人として文書化する。これがClaude Codeの最終作業である。

以後の実装はCodexが担当し、検証・指示書・承認はClaude webが担当する。引き継ぎ書はCodexが新しいセッションの最初に読む「再開位置の正本」となる。

## ■ 作業手順

### Step 1: push

コミット e001031 をpushすること（承認済み）。push後 `git log --oneline -3` を確認。

### Step 2: 引き継ぎ書の作成

`ArchitectureRepository/00_Master/トリガー作成指示書群/ORDER_FLOW_STRATEGY_ENGINE_HANDOVER_TO_CODEX_20260727.md` を新規作成すること。

以下の構成を必須とする:

#### §1 現在地（事実のみ、ファイル:行番号または実測値で根拠を示す）
- ブランチ・最新コミットハッシュ・push状態
- テスト実測値（全体 574 passed, 1 skipped の内訳: 既存516 / 契約拒否27+1skip / engine 31）
- 実行コマンド（`python -m pytest -q -p no:cacheprovider`、実行ディレクトリ）
- 収録状況（DOM 7/29終了予定、清算 8/9終了予定、Hook Stage 2A/2B append-only収録が稼働中で停止禁止）

#### §2 完了済み4工程の要約
各工程について: 目的 / 成果物（ファイルパス一覧）/ コミットハッシュ / checkpoint文書名。

#### §3 アーキテクチャ関係図
以下の関係をテキスト図で示すこと:
- `src/strategy_contract/`（契約oracle、拒否試験の参照実装）
- `src/strategy_engine/`（本体骨格、enforcerを内部で呼ぶ合成方式）
- `src/strategy_engine/ingestion/`（MarketStateSnapshot → Tier A condition snapshot写像層）
- データフロー: MarketStateSnapshot → IngestionAdapter → PredicateObservation.conditions → RealPredicateEvaluator（CalibrationBook参照）→ Engine → ContractEnforcer.submit() → StepResult / OrderReadyHandoff
- 「Engineは遷移判定を再実装しない（enforcerが唯一の正）」を明記

#### §4 設計判断の記録（なぜそうしたか）
最低限以下を含む:
- enforcer合成方式の採用理由（契約の自動継承、二重実装の排除）
- condition snapshot疎結合の採用理由（CD-G* keyを生成するコードが存在しない事実）
- Tier A/Tier B の2層構造と、Tier Aのみで代表1型全経路が通る事実
- CalibrationBook注入方式（閾値ハードコード禁止、fail-closed、既存ThresholdBook様式）
- lifecycle predicate（ENGINE_TERMINAL/ENGINE_EXPIRY）が構造的にCALIBRATEDである理由

#### §5 documented gap 一覧（全件、漏れなく）
- Tier B合成FLAG未生成（composite synthesis層が将来必要。反証routeが現状効かない事実と、runtime有効化0下では安全である理由を明記）
- pre-aggregated本計算（refresh/pull/progress）が snapshot producer側の将来実装であること
- price_oi_joint_state_5m のENUM数値符号化が便宜的であること（正本ENUM定義との対応確定が将来必要）
- ingestion adapterがLive/Replayに未接続であること（EventSource実装が将来工程）
- 【未定義-1】cross-instance source_event_id再利用が正本未定義であること
- 他364 variantが未実装であること
- その他、作業中に認識したgapがあれば全て記載

#### §6 次工程の候補と推奨順序
- composite synthesis層 / snapshot producer（replay統合）/ 較正ツール / 他variant一般化 の依存関係
- 較正は収録完了（DOM 7/29、清算 8/9）後が本番であること
- 推奨順序を根拠付きで記載（ただし決定はお館様が行うことを明記）

#### §7 Codexが守るべき制約・運用ルール
- runtime有効化0・発注権限0の維持（live化はお館様の明示指示まで封印）
- 完成済み機能・既存検出器・正本CSV/ポリシー・raw data・収録基盤の変更禁止
- fail-closed運用: 齟齬・欠測・未定義を発見したら勝手に仕様を決めず報告して停止
- 1工程1指示書・段階停止（Stage 1設計提案 → お館様OK → Stage 2実装 → 完了報告）
- pushはお館様の指示があるまで行わない
- すべての主張はファイル:行番号または実測値で根拠を示す。根拠なき断定禁止
- 回帰保証: 全工程で既存テスト全件PASSを完了条件とする

### Step 3: 引き継ぎ書のコミットとpush

引き継ぎ書を単独コミットとし、push すること（本指示書で事前承認済み）。
コミットメッセージ: `docs: handover Strategy Engine work to Codex`

### Step 4: 完了報告

以下を報告して停止:
- push結果（Step 1 / Step 3 両方）
- 引き継ぎ書のファイルパス
- `git log --oneline -5`
- 最終テスト実測値

## ■ 制約

- コード変更は一切行わない（文書作成とpushのみ）
- 引き継ぎ書の内容は実物（ファイル・コミット・テスト結果）に基づくこと。記憶や推測で書かない
- 正本CSV/ポリシー文書の変更禁止
- 収録基盤の停止・変更禁止
