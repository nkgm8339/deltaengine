# Claude Code 指示書：push前整理

作成日: 2026-07-27 JST
前提: Replay契約拒否試験 Stage 2 完了承認済み（コミット aa1f43e、push未実施）

## ■ 目的

クリーン環境で全テスト（既存516件＋新規27件＋1 skip）が再現可能な状態にしてからpushする。

## ■ 作業手順（この順番で実施）

### Step 1: 正本CSV群のコミット

`ArchitectureRepository/00_Master/トリガー作成指示書群/` 配下の正本CSV群（FSM・binding・routing・predicate registry）を、Replay契約拒否試験コミットとは**別コミット**として先に積むこと。

- コミットメッセージ: `add: Strategy Engine canon CSV registries (FSM/binding/routing/predicate)`
- 同ディレクトリ配下の正本ポリシー文書（.md）も未追跡であればこのコミットに含めてよい
- 変更ではなく追加（新規追跡）であることを `git status` で確認してからコミットすること

### Step 2: PROJECT_MEMORY.md の差分報告

PROJECT_MEMORY.md に前段作業由来の未コミット変更（222行）がある。**コミットせず**、差分の内容をお館様に報告すること。

報告形式:
- `git diff PROJECT_MEMORY.md` の出力（全文）
- 変更の概要（何が追加/削除/変更されたか）を3行以内で要約

お館様が内容を確認してからコミット可否を判断する。

### Step 3: 一時ディレクトリの扱い

`Delta_Engine_Pro4web/.test_tmp_stage2b_*` の5ディレクトリは**コミットに含めない**。

- 既に `.gitignore` に記載があるか確認すること
- 記載がなければ `.test_tmp_stage2b_*` を `.gitignore` に追加し、そのgitignore変更をStep 1のコミットに含めるか、別の小コミットとすること
- ディレクトリ自体の削除はしない（Stage 2B試験の一時物であり、削除はお館様の判断）

### Step 4: コミット履歴の確認

Step 1〜3 完了後、`git log --oneline -5` でコミット順序を報告すること。期待される順序:

1. 正本CSV群の追加コミット（Step 1）
2. Replay契約拒否試験コミット aa1f43e（既存、rebase等で順序入替が必要なら報告して停止）

### Step 5: 全テスト実行

`python -m pytest -q -p no:cacheprovider` を実行し、結果を報告すること。期待: 543 passed, 1 skipped。

### Step 6: push前停止

**pushは実行しない。** Step 1〜5 の結果をまとめて報告し、お館様のpush指示を待つこと。PROJECT_MEMORY.md の差分報告（Step 2）の判断結果を反映してからpushする。

## ■ 制約

- 正本CSV/ポリシー文書の内容変更禁止（追跡開始のみ）
- PROJECT_MEMORY.md はコミットしない（差分報告のみ）
- 一時ディレクトリはコミットに含めない
- 既存コードの変更禁止
- rebase/force-push が必要になる場合は実行せず報告して停止
- 収録中のHook Stage 2A/2B観測基盤の停止・変更禁止
