# お館様承認：Stage 2A 着手承認（2026-07-26）

対象: HOOK_DETECTOR_TRIGGER_OBSERVE_DESIGN_PROPOSAL_20260726.md（第1段階設計提案）

## 承認内容

Stage 2A の着手を承認する。append-only収録と Hook共通契約から開始してよい。

設計提案の以下の方針を承認する。

- DOM・清算履歴は現状のままでは分位較正に使用不可 → append-only収録後に閾値を生成する
- 未較正Hookは発火禁止
- A/C/D/E/F/G と既存Hook adapter を独立層として追加
- T01〜T50中46型を搭載（T17/T34/T35/T36はB09/B10/B13/B14実装後に追加）
- 全型OBSERVE固定。Flow単体発注とLIVE注文は禁止

## 承認条件（必須）

1. **較正に必要な最低収録期間の見積もりを提示すること。** DOM分布・清算分布それぞれについて、閾値生成に足ると判断する収録日数とその根拠を、次回報告に含める。無期限収録は不可。
2. **収録待ちで作業を止めないこと。** 収録は開始後バックグラウンドで継続し、その間に検出器・トリガー並走基盤の実装を並行して進める。収録完了→閾値生成→較正済みフックの発火解禁、という流れで、実装完了と収録完了が概ね同時に揃うよう工程を組むこと。

## 従前の制約（再掲・継続）

- 既存の検出器・パイプライン・ストレージ・WebApp・稼働中システムの動作を変更しない
- 記録の改変・削除は一切しない
- 各段階完了ごとに報告し、お館様の確認を得てから次へ進む
