# 指示書_Heatmap_是正_v2
**作成日: 2026-07-29 / 発行: Claude(統括) / 対象: Claude Code**
**前提: 指示書_Heatmap_是正_v1 の Task A は方式変更により置き換え。Task Bはそのまま有効。**

---

## 変更理由

v1のTask A(38文書への個別追記)は、Windows sandboxが既存追跡ファイルの編集を拒否するため実行不能と判明した(2回連続エラーで正しく停止済み)。
新規ファイル作成は成功しているため、**個別追記をやめ、単一の無効化インデックス文書を新規作成する方式**に変更する。

---

## Task A2: 無効化インデックス文書の新規作成

### 手順

1. 以下のパスに新規ファイルを作成する(既存ファイルの編集は行わない):

`ArchitectureRepository/00_Master/HEATMAP_INVALID_CLAIMS_INDEX_20260729.md`

2. 内容は以下のテンプレートに、Phase 0報告で列挙された**追記できなかった38文書**のパスを機械的に列挙する。

```markdown
# Heatmap関連 完成主張 無効化インデックス
作成日: 2026-07-29
根拠: Phase 0現状調査(2026-07-29)、指示書_Heatmap_是正_v1/v2

## 無効通知

以下に列挙する全文書中の「完成」「PASS」「稼働」に関する記載は、
Phase 0現状調査により**無効**と判定された。原文は訂正・削除せず、証拠として保存する。

無効と判定した根拠:
- Heatmapフラグは index.html:969 で明示的にOFF、通常画面から起動不能
- UIテスト(test_orderbook_heatmap_ui.py:9)は「Heatmap OFF」を合格条件としており、機能動作を検証していない
- 実Canvas描画の自動テストは存在しない
- 保存済み描画成果物は x座標-185.25px のバグと描画p95 31.8ms/112.1ms(予算16ms超過)を示す
- 永続writerは50段サンプリング記録でありADR-011(全量記録)に違反
- writerの毎フレーム同期write+flush+fsyncはADR-003(単一ループ非同期)に違反

正本は HEATMAP_指示書(最新版) とする。

## 対象文書一覧(無効化対象)

(ここに38文書のフルパスを1行1文書で列挙する。Phase 0報告に記載された
「追記できなかった対象38文書」リストをそのまま転記すること。改変・取捨選択禁止)

## 対象外と判断した文書

- ArchitectureRepository/AA_仕様書_LiquidityHeatmap_WebUI_v1.md
  理由: 設計案であり完成・稼働の主張文書ではないため
```

3. sandboxエラーが発生した場合(新規作成でも失敗する場合)は再試行を1回のみ許可する。2回連続失敗したら停止し、エラーメッセージ全文を報告する。

---

## Task B: Gitベースライン確保(v1から継続、変更なし)

v1のTask B手順をそのまま実行する。ただし対象ファイル一覧に以下を追加する:

- `ArchitectureRepository/00_Master/HEATMAP_INVALID_CLAIMS_INDEX_20260729.md`(Task A2成果物)
- `ArchitectureRepository/00_Master/指示書_Heatmap_是正_v1.md`
- `ArchitectureRepository/00_Master/HEATMAP_指示書_v1.2_20260729.md`
- `CLAUDE.md`

既存の `git status --porcelain` 結果にある **M(変更済み)ファイル10件**については、以下の対応とする:

- これらは既存ソース・仕様書の変更であり、本是正指示書の範囲外の変更が含まれている可能性がある
- コミットには含めず、`git diff` の要約(ファイルごとの変更行数と概要)のみを報告に含める
- 変更内容の是非は次工程(Phase 1設計)で判断するため、**変更を破棄しない**

対象は **未追跡(??)ファイルのみ** を1コミットに含める。

---

## Task C: 完了報告

```text
[完了/失敗/停止] 指示書_Heatmap_是正_v2

## Task A2
- HEATMAP_INVALID_CLAIMS_INDEX_20260729.md の作成結果(成功/失敗)
- 列挙した対象文書数(38件と一致するか)

## Task B
- コミットハッシュ
- コミットに含めたファイル数(未追跡ファイルのみ)
- M(変更済み)ファイル10件の diff 要約(ファイル名・変更行数・変更概要)

## 逸脱事項
- なければ「なし」
```

---

## 禁止事項(再掲)

- 既存ソースコード・既存Markdown文書の編集(sandbox制約により不可と確認済み)
- テストの実行
- ライブ接続
- Task B以外のgit操作(rebase/reset/push等)
- M(変更済み)ファイルのコミットまたは破棄
- 次フェーズ(Phase 1設計)への着手
