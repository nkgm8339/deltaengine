# 00_プロジェクト引継ぎ書_v3.19
## Order Flow Analysis Platform Architecture Repository

> 母体は `00_プロジェクト引継ぎ書_v3.18`。本書は次セッションで**課題5(stack_ref・min_volume 実測較正)と課題6(cvd_slope_ref 再較正)を実行する**ための作業スコープ限定引継ぎ。

---

# 0. お館様からの最重要指令(絶対遵守)

> **効率よく完璧に、バグなく指示書作成しろ。自己保身のために必要のない作業は絶対するな。クレジットを節約しろ。必要十分で指示をすること。二度と自己保身のために冗長することがないようにしろ。最短で完全デバグのない状態を要求する。**

---

# 1. 現在地(母体 v3.18 準拠)

- DeltaEngine v3.6.6 / テスト 348 passed / Docker 本番起動クローズ済
- 場所: `C:\Users\user\Desktop\DeltaEngine`(git 管理)。コードは `Delta_Engine_Pro4web/` 配下
- 実装体制: Claude Code 不調のため web 直接実装

---

# 2. このセッションでやること

**較正2件。新規実装は不要。ツールは実装済み(v3.6.4 / v3.6.2)、残っているのは実行と反映と確認。**

## 課題5 — stack_ref・min_volume 実測較正(今すぐ実行可)

- ツール: `tools/calibrate_refs.py`(v3.6.4 実装済み)
  - `imbalance.min_volume` … レベル総出来高 **P25**
  - `signal.stack_ref` … stacked run 長 **P80**(実運用と同一の ImbalanceDetector で計測)
  - `--write` で config へ反映
- 対象 config: `config/config.yaml` の `imbalance.min_volume`(現 0.5)/ `signal.stack_ref`(現 3)
- 狙い: シグナル飽和の是正。確定知見どおり `stack_ref=3` は 3スタック1本で ±100 到達=飽和しやすい。実測分布で妥当値へ。

## 課題6 — cvd_slope_ref 再較正(**データ蓄積が前提。ゲートあり**)

- ツール: `tools/calibrate_cvd.py`(既存)。`--write` で反映
- 対象 config: `config/config.yaml` の `signal.cvd_slope_ref`(現 65.203)
- 現状: 65.203 は v3.6.2 で直近7日 |delta| P80 から較正。ただし**サンプル 397 件と薄い**
- **ゲート条件**: サンプルが十分件数に育ってから実行する。397 件前後のままなら再較正しても薄い較正の再生産にしかならない。件数が不足なら**課題6は実行せず、課題5のみで打ち切る**

---

# 3. 実行前に必ず確認すること(グラウンディング)

1. `tools/calibrate_refs.py` と `tools/calibrate_cvd.py` の**正確な CLI**(データソース指定・期間・`--write` 以外のフラグ)を、ツール本体か `--help` で確認してから走らせる。フラグを推測で叩かない。
2. 較正は**実データ**が要る。ライブ収集済みデータ or 収集手順(母体 §で live 経路は実装済み)を先に確認。
3. `--write` は config を書き換える。**実行前に現行 config 値(min_volume=0.5 / stack_ref=3 / cvd_slope_ref=65.203)を控える**。

---

# 4. 完了条件

- 課題5: 推奨値算出 → `--write` 反映 → 変更後 config を確認 → シグナル頻度が妥当化したか目視/機械で確認
- 課題6: **ゲート成立時のみ**同手順。不成立なら「サンプル不足で見送り」を明記して未実行のまま残す
- CHANGELOG に較正エントリ追記(バージョン規律。UI 表示が自動追従)
- CompletionLog に実施記録(較正前後の値・サンプル件数・逸脱有無)
- 較正は config 値の変更。コード変更が発生した場合のみ `float(` スキャン + pytest green を追加確認

---

# 5. 運用ルール(母体 v3.18 §4 継承・要点)

- web 直接実装時の完了条件: pytest green + `float(` スキャン + 配信 HTML 機械検証 + 修正ファイル納品 + 配置手順の提示
- 納品は内容全文埋め込みの ps1 方式(絶対パス / 実行後 `git rm`)
- バージョン規律: ドキュメント変更時は CHANGELOG に新エントリ追記
- 応答規範: 結論ファースト・簡潔・文末は「お館様」

---

# 6. 次セッション開始文

```text
引継ぎ書_v3.19 を読んでください。課題5から実行。GO
```
