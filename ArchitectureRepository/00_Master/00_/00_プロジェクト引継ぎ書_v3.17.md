# 00_プロジェクト引継ぎ書_v3.17
## Order Flow Analysis Platform Architecture Repository

---

# 0. お館様からの最重要指令(絶対遵守)

> **効率よく完璧に、バグなく指示書作成しろ。自己保身のために必要のない作業は絶対するな。クレジットを節約しろ。必要十分で指示をすること。二度と自己保身のために冗長することがないようにしろ。最短で完全デバグのない状態を要求する。**

---

# 1. プロジェクト状態

**Docker 本番起動確認クローズ + v3.6.3(バージョン表示機能)実装済み。テスト 291 passed。`docker-compose up` 一発起動の当初目標に到達。**

- 場所: `C:\Users\user\Desktop\DeltaEngine`(git 管理下)
- コミット系譜: `0677b98`(baseline)→ … → `fedff82`(ワンショットスクリプト除去)→ **v3.6.3 コミット(本引継ぎ書配置後に実施)**
- 正常スナップショット ZIP: `DeltaEngine_snapshot_v363.zip`(v3.6.3 実装前ベース)
- 保険フォルダ(削除禁止): `C:\Users\user\Desktop\DeltaEngine_broken_20260718`

---

# 2. 経緯(v3.16 からの差分のみ)

## Docker Desktop 復旧完了(2026-07-18)

- DISM RestoreHealth 後の CheckHealth で「破損は検出されませんでした」を確認(14098 の根本原因は解消)
- しかし起動時に **レジストリキー欠損**(`Error: getting backend binary path: cannot find registry key "SOFTWARE\Docker Inc.\Docker Desktop"`)が発覚。コンポーネントストア破損時にインストール情報ごと壊れたと判断
- **クリーン再インストール**で解消: `Docker Desktop Installer.exe uninstall`(4.82.0)→ 残骸フォルダ削除(Program Files / LOCALAPPDATA / APPDATA)→ `wsl --unregister docker-desktop` / `docker-desktop-data` → 最新版(4.81.0)再インストール → 再起動
- `docker version` Client/Server 両表示、`docker run --rm hello-world` 成功

## `docker-compose up` 本番起動確認(2026-07-18)— 保留課題 7 クローズ

- `docker-compose up --build` → `http://localhost:8080` 目視合格
- 確認項目: 3カラム UI / オーダーブック更新 / フットプリント昇順 + [CLOSED]+Ns(TaskF 反映)/ シグナル連続値 CVD +21・FOOTPRINT +61(Phase1 反映)/ CONFLUENCE 星点灯(TaskC 反映)/ CVD+Δ チャート描画

## v3.6.3 — アプリバージョン表示機能(web 直接実装)

- **単一情報源**: `ArchitectureRepository/00_Master/CHANGELOG.md` の最新エントリ見出し(`# vX.Y.Z`)。コードへの手動転記禁止
- **仕組み**: `webapp/version.py`(パーサ+候補パス走査、失敗時 `v?.?.?` フォールバック)→ lifespan で起動時 1 回解決 → `GET /api/version` → `index.html` の `#appver`(topbar 右端)に表示
- **Docker 対応**: `docker-compose.yml` で CHANGELOG.md を `/app/CHANGELOG.md:ro` にマウント(ビルドコンテキスト外参照のため volume 方式)
- **システム的バージョンアップの成立**: Documentation-First 規律により仕様書変更 → CHANGELOG 追記 → 次回起動時に UI バージョン自動昇格
- 回帰テスト 6 本追加。**285 → 291 passed**
- テスト知見: モジュールレベル `app` は他テストの lifespan 実行で `app.state` が残留する(順序汚染)。エンドポイントのフォールバックパスを検証するテストは `app.state.version = None` で明示リセットする

---

# 3. 確定済み技術知見(再調査禁止)

- IMBALANCE スコア ±100 到達は仕様(stack_ref=3 で 3 スタック 1 本=±100)。頻度過多なら stack_ref を 5〜6 へ。
- cvd_slope_ref はサンプル 397 件と少なめ。データ蓄積後 `python tools\calibrate_cvd.py --write` で再較正推奨。
- フットプリントは確定バーのみ配信(ライブ進行中バーなし)。板との時間差は仕様、[CLOSED]+Ns で明示済み。
- git の実パスは `Delta_Engine_Pro4web/webapp/...`(リポジトリルートは DeltaEngine 直下、コードは Delta_Engine_Pro4web/ 配下)。
- CRLF 警告・改行コード差分は無害(Windows 環境由来)。
- **Docker ビルドコンテキストは `Delta_Engine_Pro4web/` のみ**。ArchitectureRepository のファイルをコンテナから参照する場合は volume マウント方式(COPY 不可)。
- バージョン表示の変更を反映するには**コンテナ再起動が必要**(起動時 1 回解決の設計。ホットリロード不要と判断)。

---

# 4. 運用ルール(厳守)

- 指示書: 1 文書=1 フェーズ / PowerShell ネイティブ+絶対パス / コマンド改変禁止 / 途中承認なし・完了時のみ報告 / git diff による事後検証。
- `DeltaEngine` フォルダ自体の Rename/Remove 禁止(Code の cwd 制約)。復元は「ZIP を _zipref に展開 → robocopy /PURGE /IS /IT /XD data .git __pycache__」方式。
- 検証原則: 機械検証できることをユーザー目視に頼らない。目視は最終の見た目確認のみ。
- **web 直接実装時の完了条件**: pytest green + float( スキャン + 配信 HTML 機械検証 + 修正ファイル納品 + 配置手順の提示。
- 納品は**内容全文埋め込みの ps1**方式(ヒアストリング貼り付け禁止 / `Set-Location` 絶対パス指定 / 実行後に `git rm` で除去)。手順メッセージには必ずファイルを再掲。
- **バージョン規律(v3.6.3 より)**: 仕様書・ドキュメント変更時は必ず CHANGELOG に新バージョンエントリを追記する(UI 表示が自動追従)。コードへのバージョン手動転記は禁止。
- web の応答規範: 結論ファースト・簡潔、判断が必要な分岐のみ提示、文末は「お館様」。

---

# 5. 次の課題(保留中・着手指示があるまで動かない)

1. ライブ進行中バーの配信(板との時間差の根本解消)
2. マルチシンボル(ETH 等: config 変更+min_volume 再較正でほぼ可能。symbol→streams 自動生成が望ましい)
3. 他取引所プロファイル(profiles/ 方式で拡張可)
4. MT5 経由 XAUUSD tick フロー実験
5. stack_ref・min_volume の実測較正
6. cvd_slope_ref 再較正(データ蓄積後)
7. Code 復旧確認(`claude update` → `claude doctor`)と分業体制への復帰判断
