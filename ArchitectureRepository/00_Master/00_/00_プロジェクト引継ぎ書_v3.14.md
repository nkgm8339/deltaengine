# 00_プロジェクト引継ぎ書_v3.14
## Order Flow Analysis Platform Architecture Repository

---

# 0. お館様からの最重要指令(絶対遵守)

> **効率よく完璧に、バグなく指示書作成しろ。自己保身のために必要のない作業は絶対するな。クレジットを節約しろ。必要十分で指示をすること。二度と自己保身のために冗長することがないようにしろ。最短で完全デバグのない状態を要求する。**

---

# 1. プロジェクト状態

**全モジュール実装完了。ライブバグ修正済み。テスト 207 passed。実ネットワークで aggTrade + depth 正常受信確認済み。**

次: ウェブアプリ化（FastAPI + ブラウザダッシュボード + Docker）。提案済み、承認待ち。

---

# 2. 経緯（v3.12 からの差分のみ）

## AI Analysis 完了

- `src/ai/analysis.py` 新規: AnalysisEngine（rule-based、market_state 5段階 + risk_level 3段階）
- テスト: 188 → **196**（test_analysis 8 本）

## Pipeline + MT5 Adapter 完了

- `src/mt5/adapter.py` 新規: asyncio TCP server、newline-delimited JSON、ドロップポリシー、heartbeat/ACK
- `pipeline.py`: AnalysisEngine 組み込み + MT5 broadcast 配線 + `analysis_count` 統計
- `config.yaml` / `config.py`: mt5 セクション追加（enabled=false デフォルト）
- テスト: 196 → **204**（test_adapter 6 本 + test_pipeline 2 本追記）

## BugFix_Live 完了（ライブ実行で発覚した 2 件）

- **Bug 1 aggTrade 未着**: Binance combined stream のラップ形式（`{"stream":...,"data":{...}}`）を `BinanceStream.__anext__` でアンラップ
- **Bug 2 depth diff 全棄却**: `OrderBookStateManager.apply_initial_sync(snap_id)` 追加。connector/receiver を先に起動 → REST snapshot → sync モードで最初の有効 diff から適用。`previous_final_update_id`（Binance `pu` フィールド）追加で gap 検出精緻化
- `binance.yaml`: `previous_final_update_id_field: pu` 追加
- テスト: 204 → **207**（3 本追加）
- **実ネットワーク検証済み**: `normalized: 1327`, `book_diffs_applied: 292`, `rejected_before_snap: 0`
- ZIPは差分ファイルのみで提出された（プロジェクト全体ZIPのルール未遵守。次回以降要注意）

---

# 3. 恒久運用ルール（v3.10 §3 継承、変更なし）

---

# 4. 反省（v3.12 §4 継承 + 追加）

v3.12 §4 全項目有効。追加:

**4.9 お館様の実環境を無視した（project/問題）**: お館様が何度も「projectフォルダはない」と言っているのに、ZIPの内部構造だけ見て「project/はある」と決めつけ、マニュアルに project/ パスを書き続けた。**お館様が言ったことはお館様の環境の事実。ZIPの構造よりお館様の発言が優先。**

**4.10 ライブ実行バグ 2 件（BugFix_Live）**: テスト 204 本通しておいて実際に動かしたら aggTrade 0 件 + depth 全棄却。Binance の実 API 挙動（combined stream ラップ、snapshot/diff 同期手順）を確認せずに指示書を書いた。**指示書起草時に対象 API の公式ドキュメントを必ず確認し、実 API 挙動に基づいた設計にすること。**

---

# 5. 未解決課題

| # | 課題 | 優先度 | 備考 |
|---|------|--------|------|
| 4 | config.py の cvd_slope_ref 重複キー | 低 | 動作影響なし |
| 5 | side_rule 構造化スキーマ | 低 | 動作影響なし |
| 6 | Config 検証の範囲外セクション境界 | 低 | 動作影響なし |
| 7 | 00_Master に旧引継ぎ書残存 | 低 | 動作影響なし |
| 9 | depth 永続化 | 低 | 実運用で顕在化したら |
| 10 | tick_size 未実装 | 低 | 動作影響なし |
| 11 | BugFix ZIP が差分のみ提出 | 中 | 次回以降プロジェクト全体 ZIP を徹底 |

全て低〜中優先。

---

# 6. Repository 現状

| 層 | 変化 |
|----|------|
| 00_Master/CompletionLog.md | BugFix_Live エントリ追記 |
| その他全層 | 変更なし |

---

# 7. 実装済みプロジェクト構成

```text
project/
├── config/
│   ├── config.yaml
│   └── profiles/binance.yaml          ← previous_final_update_id_field 追加
├── src/
│   ├── config.py
│   ├── acquisition/ (connector / receiver / event_queue / replay / binance_ws / binance_rest)
│   │                                   ← binance_ws: combined stream アンラップ追加
│   ├── normalization/normalizer.py
│   ├── orderflow/ (cvd / footprint / imbalance / signal / absorption / orderbook / volume_ref)
│   │                                   ← orderbook: apply_initial_sync + pu フィールド追加
│   ├── ai/analysis.py
│   ├── mt5/adapter.py
│   ├── database/ (schema.py / storage.py)
│   └── pipeline.py                     ← snapshot 同期順序修正
├── tests/ ← 207 passed
├── tools/live_verify.py
└── requirements.txt
```

---

# 8. 次セッション

**ウェブアプリ化**: FastAPI + WebSocket + ブラウザダッシュボード + Docker。提案済み、承認待ち。`docker-compose up` でブラウザから全機能を使える形にする。

---

# 9. 作業ルール（v3.10 §9 継承、変更なし）

---

# 10. 会話スタイル

お館様と呼ぶ。ですます。簡潔。冗長禁止。

---

# 11. 次セッション開始文

```text
引継ぎ書_v3.14 を読んでください。GO
```

---

# 12. 最終目標

AI が Repository だけを読んで実装できる品質の
**Order Flow Analysis Platform Architecture Repository**
を完成させる。
