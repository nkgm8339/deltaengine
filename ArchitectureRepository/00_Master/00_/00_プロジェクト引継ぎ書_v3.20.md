# 00_プロジェクト引継ぎ書_v3.20

## Order Flow Analysis Platform Architecture Repository

> 母体は v3.19。本書は **ADR-011(全量記録の原則)を確定・配置した直後**の引継ぎ。次セッションは板永続化の実装設計に入る。

---

# 0. お館様最重要指令(絶対遵守)

> 効率よく完璧に、バグなく。自己保身の不要作業禁止。クレジット節約。必要十分。最短で完全デバグ済みを要求。先走らない・都度提案・ですます・文末は「お館様」。

---

# 1. 現在地

- DeltaEngine v3.6.6 / テスト 348 passed / Docker 本番起動クローズ済
- 場所: `C:\Users\user\Desktop\DeltaEngine`(git 管理)。コードは `Delta_Engine_Pro4web/` 配下
- 正本 ADR: `...\ArchitectureRepository\00_Master\ADR\`(ADR-000〜011 実在。011 を本セッションで追加・配置 OK 確認済)
- **実装体制は要再確認**: 記録上は「web 直接実装(Code 不調)」だが、お館様は「ADR 更新は Codex がやっている」と発言。**現在の実装担当が web か Code/Codex か、次セッション冒頭で必ず確認すること。**

---

# 2. 本セッションでやったこと

**ADR-011「全量記録の原則(No-Decimation Recording Principle)」を新規制定・配置(OK 確認済)。**

- 動機: お館様の思想。板を out-of-scope で捨てた v3.6.0/M11 判断を原則から見直す。「一寸先は分からない相場では、その場の判断でデータを間引くこと自体が危険。大元の生を全量残し後から分析する」
- 三軸で「間引かない」を定義: **時間**(後から見るため全量)/**表示**(蓄積を見せ方に従属させない。クラシックも新表示も同一蓄積の下流)/**加工**(土台は取引所の生データ時系列。加工物は派生でいつでも再計算可)
- 生の定義: **取引所から届いた未整形メッセージそのものを最優先**。正規化後は「捨てた情報が復元不能」なので次善
- 欠損の非隠蔽: 生のまま残せば欠損が欠損として見える
- 実装は段階的: **Order Book 永続化から着手**。過剰な先回り禁止
- ファイル: `ADR-011_No_Decimation_Recording_v3.0.md`(Status: Accepted)を PowerShell の here-string で配置。実行後 `OK` 表示を確認済

---

# 3. 次セッションでやること

## 未完(本セッションの積み残し)
- **CHANGELOG 追記が未実施**。ADR 追加はバージョン規律上 **v3.6.7 相当**。`...\00_Master\CHANGELOG.md` に ADR-011 制定エントリを追記(UI バージョン表示が自動追従)

## 本題: 板永続化の実装設計(ADR-011 軸3 の最初の実装)
- **コード実物確認が先**。推測で設計しない。特に:
  1. 正規化(`src/normalization/normalizer.py`)が生データの**何を保持し何を捨てているか**。「生」をどこで掴むかの確定に直結
  2. 板 diff がパイプラインを流れる形(`src/pipeline.py` / `src/orderflow/orderbook.py`)
  3. 既存リプレイ経路(YAMLReference §5 / pipeline replay / ReplayStats)が何を入力に取るか
- 粒度の決定: 生メッセージのべた書き か 周期snapshot+diff か。お館様は「全部見たい/生一択」志向 → べた書き寄りで提案
- 到達目標: 板をリプレイで再生し、板ヒートマップと Absorption が過去再生でも埋まる

---

# 4. グラウンディング(必須)

- 現在地は記憶でなく**お館様提供のファイルと実物**を根拠にする
- 較正の積み残し(v3.19 由来): 課題5(stack_ref・min_volume)は 2026-07-20 反映済(3→5 / 0.5→0.002)。課題6(cvd_slope_ref 65.203)はサンプル薄く見送り中

---

# 5. 開始文

```text
引継ぎ書_v3.20 を読んでください。まず現在の実装担当(web か Codex か)を確認。次に CHANGELOG 追記、その後 板永続化の実物確認。GO
```
