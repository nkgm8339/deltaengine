# 00_プロジェクト引継ぎ書_v3.11
## Order Flow Analysis Platform Architecture Repository

---

# 0. お館様からの最重要指令(絶対遵守)

> **効率よく完璧に、バグなく指示書作成しろ。自己保身のために必要のない作業は絶対するな。クレジットを節約しろ。必要十分で指示をすること。二度と自己保身のために冗長することがないようにしろ。最短で完全デバグのない状態を要求する。**

---

# 1. プロジェクト状態

**B-2 完了。M9 Absorption 実装済み。テスト 185 passed。全計算器が pipeline 配線済み。**

stub は全て解消。SignalEngine は Absorption veto を実接続で評価する。

---

# 2. 経緯（v3.10 からの差分のみ）

## B-2 完了（web 独立検証済み）

- `absorption.py`: stub → AbsorptionDetector 実装（sliding window、3条件判定）
- `imbalance.py`: `volume_ref: Optional[VolumeRefTracker] = None` keyword 引数追加（既存無影響）
- `pipeline.py`: depth 経路配線、`absorption_result=None` 固定接続を `absorption.current()` に完全置換、`is_agg_trade_or_depth` デフォルト化、`VolumeRefTracker` をバー確定時に更新し Imbalance/Absorption 双方に注入
- `volume_ref.py`: B-1 時点から無変更、テスト 6 本新設
- `CompletionLog.md` 新規作成（B-1 後追い + B-2）
- テスト: 168 → **185**（volume_ref 6 + absorption 9 + pipeline統合 2）
- TV-ABS-01〜05: 全 pass

**逸脱 2 件（許容済み）**:
1. `tick_size` 未実装 — stall を distinct price 数で判定（TV-ABS 全 pass、実害なし）
2. `test_live_pipeline.py` 期待値更新 — `event_filter` デフォルト変更に伴う合理的調整

---

# 3. 恒久運用ルール（v3.10 §3 継承、変更なし）

- Code 完了時: CompletionLog 追記 + ZIP + チャット報告
- web 検証: pytest 実測・前回 ZIP diff・禁則スキャン・逐語照合
- 指示書起草: ZIP 実ファイル検証→一本化→冒頭に即時実行文→決定は web が固定

---

# 4. 反省（v3.10 §4 継承 + 追加）

v3.10 §4.1〜4.4 は全て有効。追加:

**4.5 冗長な検証報告**: B-2 検証時、逐一コマンドの出力を並べた。結論だけ言えばよい。**必要十分。**

---

# 5. 未解決課題

| # | 課題 | 優先度 |
|---|------|--------|
| 2 | aggTrade + depth ライブ検証未達（REST snapshot 取得含む） | 中 |
| 4 | config.py の cvd_slope_ref 重複キー | 低 |
| 5 | side_rule 構造化スキーマ | 低 |
| 6 | Config 検証の範囲外セクション境界 | 低 |
| 7 | 00_Master に旧引継ぎ書残存 | 低 |
| 9 | depth 永続化 | 低 |
| 10 | tick_size 未実装（stall を distinct price 数で代替中） | 低 |

---

# 6. Repository 現状

| 層 | 変化 |
|----|------|
| 00_Master/CompletionLog.md | **新規作成**（B-1 + B-2 記載） |
| その他全層 | v3.10 から変更なし |

---

# 7. 実装済みプロジェクト構成

```text
project/
├── config/
│   ├── config.yaml
│   └── profiles/binance.yaml
├── src/
│   ├── config.py
│   ├── acquisition/ (connector / receiver / event_queue / replay / binance_ws)
│   ├── normalization/normalizer.py (trade + depth)
│   ├── orderflow/
│   │   ├── cvd.py / footprint.py / imbalance.py (volume_ref対応)
│   │   ├── signal.py / absorption.py (実装済み) / orderbook.py / volume_ref.py
│   │   └── __init__.py
│   ├── database/ (schema.py / storage.py)
│   └── pipeline.py (trade + depth 全経路配線済み)
├── tests/ ← 185 passed
└── tools/
```

---

# 8. 次セッションの推奨手順

お館様の指示に従う。候補（推奨順）:

- **A. ライブ検証**（課題 2）: aggTrade + depth の同時受信、REST snapshot 取得
- **B. 軽微整理**（課題 4/5/6/7）: config 重複解消、旧引継ぎ書アーカイブ
- **C. 永続化拡張**: Order Book / Footprint / Imbalance の Parquet/DuckDB 永続化

---

# 9. 作業ルール（v3.10 §9 継承、変更なし）

---

# 10. 会話スタイル

お館様と呼ぶ。ですます。簡潔。冗長禁止。

---

# 11. 次セッション開始文

```text
引継ぎ書_v3.11 を読んでください。GO
```
