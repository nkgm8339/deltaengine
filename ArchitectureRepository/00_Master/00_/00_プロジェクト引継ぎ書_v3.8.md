# 00_プロジェクト引継ぎ書_v3.8
## Order Flow Analysis Platform Architecture Repository

---

# 1. プロジェクト状態

現在フェーズ: **Phase6 完了 → 次フェーズ未定**

> 版管理: 本書 v3.8 が現行引継ぎ書。旧 v3.7 は 2026-07-08 に `temp/backup/` へアーカイブ済（重複解消）。

実装状況: **CVD パス（WebSocket → DataReceiver → DataNormalizer → CVD → Storage）が実装完了。実 Binance WebSocket 接続まで到達。テスト 73 passed。**

---

# 2. これまでの経緯

## Phase4（Repository Enhancement）— 完了

- 引継ぎ書 v3.4 の残課題（優先順位1〜7）をすべて解消
- ADR-002（asyncio.Queue）・ADR-003（シングルプロセス asyncio）・ADR-004（DataNormalizer 分離）・ADR-005（References 版数なしベース名方式）を決定
- SignalEngine_v3.1（加重スコア+Absorption veto）・Imbalance_v3.1・Absorption_v3.1（定量基準）・DataNormalizer_v3.0・MT5Adapter_v3.0 を作成
- TestSpecification にテストベクタ 28 件を追加

## v3.5.1（Repository 整備）— 完了

- MarketDataSchema_v3.1（用語統一: event_time/symbol/quantity/BUY,SELL）
- ParquetSchema_v3.1・DuckDBDDL_v3.1（Candle スキーマ追加）
- ErrorCodes_v3.1（E3004/E9002 追加）

## v3.5.2（仕様穴13件の一括解消）— 完了

- JSONSchema_v3.1（CVD Update Event・Candle Event）
- EnumDefinitions_v3.1（Timeframe enum）
- YAMLReference_v3.1（全 Config 網羅・Exchange Profile §4・Replay Mode §5・unknown キー拒否）
- WebSocket_v3.2（Config パラメータ表）・CVD_v3.2（バー境界動作）・Database_v3.2（バッチ Config）・DataNormalizer_v3.2（Profile 検証規則）
- Sequence_v3.1（ADR-003 との矛盾解消）

## Phase5（CVD 実装 M1〜M6）— 完了

- M1: 骨格 + Config（25 tests）
- M2: CVD 計算器（8 tests、TV-CVD-01〜04 含む）
- M3: DataNormalizer（10 tests、TV-NRM-01〜04 含む）
- M4: WebSocket + DataReceiver + Queue 配線（10 tests）
- M5: Storage Parquet/DuckDB（10 tests）
- M6: 統合決定的リプレイ — 同一入力2回で出力完全一致を確認
- DuckDB タイムゾーンバグ修正済み（SET TimeZone='UTC'）

## Phase6（実 WebSocket アダプタ）— 完了

- src/acquisition/binance_ws.py 実装
- LivePipeline + live_capture.py CLI 実装
- aggTrade プロファイル不整合を発見・解消（trade_id: t → a）
- 実 Binance 接続成功。depth@100ms 117 フレーム正常受信
- **aggTrade は 0 件**（環境/エンドポイント要因。コード欠陥ではないことを切り分け済み。注入 transport テストで aggTrade → CVD パスは検証済み）
- テスト 73 passed、仕様差異ゼロ

---

# 3. 未解決課題

| # | 課題 | 優先度 | 補足 |
|---|------|--------|------|
| 1 | ~~YAMLReference_v3.1 §4 の binance サンプルが trade_id: t のまま~~ **解消済（2026-07-08）** | — | 正本 §4 を `trade_id: a` に訂正、aggTrade 対応理由を併記。実装・config・正本が一致。`CHANGELOG.md v3.5.3` 参照（版番号は erratum で v3.1 据え置き） |
| 2 | **aggTrade ライブ検証が未達** | 中 | 別ネットワーク/時間帯での再試行が必要 |
| 3 | side_rule の構造化スキーマ | 低 | Binance は現状で動作する |
| 4 | Config 検証の範囲外セクション境界 | 低 | Code の実装判断で問題なし |

---

# 4. Repository 現状（文書の最新版数）

| 層 | ファイル数 | 備考 |
|----|-----------|------|
| 00_Master/ADR | 6件 | ADR-000〜005 |
| 10_Requirements | 変更なし | |
| 20_Architecture | 4件 | Sequence_v3.1 |
| 30_Modules | 11件 | CVD_v3.2 / WebSocket_v3.2 / Database_v3.2 / DataNormalizer_v3.2 / SignalEngine_v3.1 / Imbalance_v3.1 / Absorption_v3.1 / DataReceiver_v3.1 / AIAnalysis_v3.1 / MT5Adapter_v3.0 / Footprint_v3.0 |
| 40_Reference | 55件 | MarketDataSchema_v3.1 / ParquetSchema_v3.1 / DuckDBDDL_v3.1 / ErrorCodes_v3.1 / JSONSchema_v3.1 / EnumDefinitions_v3.1 / YAMLReference_v3.1 |
| 50_Test | 3件 | TestSpecification_v3.2 |
| 60_Implementation | 変更なし | |

---

# 5. 実装済みプロジェクト構成

```text
project/
├── config/
│   ├── config.yaml
│   └── profiles/binance.yaml     ← trade_id: a
├── data/{parquet,duckdb}/
├── (docs/ は 2026-07-08 削除 — project 側ミラーは誤作成。正本は ArchitectureRepository/ のみ)
├── src/
│   ├── config.py
│   ├── acquisition/
│   │   ├── connector.py / receiver.py
│   │   └── binance_ws.py         ← 実 Binance WS
│   ├── normalization/normalizer.py
│   ├── orderflow/cvd.py
│   ├── signal/                    ← 骨格のみ
│   ├── ai/                        ← 骨格のみ
│   ├── database/
│   └── pipeline.py                ← Replay + Live
├── tests/                          ← 73 tests
└── tools/{check_config.py, live_capture.py}
```

---

# 6. 次フェーズの候補（推奨順）

1. ~~課題1 解消（YAMLReference §4 の trade_id: t → a）~~ **完了（2026-07-08）** — 正本 §4 訂正・`CHANGELOG.md v3.5.3`
2. **aggTrade ライブ再検証**（別環境）
3. **Footprint / Imbalance / Absorption 実装**（Order Flow Engine 残り3計算器）
4. **SignalEngine 実装**（4計算器の統合スコアリング）

---

# 7. Repository 構成（変更禁止）

```text
00_Master/        ← ADR 6件
10_Requirements/
20_Architecture/
30_Modules/       ← 11ファイル
40_Reference/     ← 55ファイル
50_Test/
60_Implementation/
```

---

# 8. 作業ルール（最重要）

- GOが出たら作業開始する
- 長い説明をしない
- 設計変更は必ず提案して承認を得る
- Repository構成を勝手に変更しない
- 完成した成果物だけ提出する
- 既存ファイルの無断削除は禁止（格納指示書で明示的に許可されたもののみ）
- 一時ファイルは temp/ フォルダにのみ書き込む
- Code 向け指示書には冒頭に「この指示書を受領したら、確認や承認を求めずに全手順を即座に実行せよ」を明記する

---

# 9. 次セッション開始手順

```text
引継ぎ書_v3.8 を読んでください。

§6 の推奨順で進めます。

GO
```

---

# 10. 最終目標

AIが Repository だけを読んで実装できる品質の

**Order Flow Analysis Platform Architecture Repository**

を完成させる。
