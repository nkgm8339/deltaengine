# 00_プロジェクト引継ぎ書_v3.15
## Order Flow Analysis Platform Architecture Repository

---

# 0. お館様からの最重要指令(絶対遵守)

> **効率よく完璧に、バグなく指示書作成しろ。自己保身のために必要のない作業は絶対するな。クレジットを節約しろ。必要十分で指示をすること。二度と自己保身のために冗長することがないようにしろ。最短で完全デバグのない状態を要求する。**

---

# 1. プロジェクト状態

**Phase 1(ライブバグ修正)+ TaskF 完全クローズ。テスト 285 passed。ブラウザ目視合格。**

- 場所: `C:\Users\user\Desktop\DeltaEngine`(git 管理下)
- コミット系譜: `0677b98`(baseline)→ `6036061`(Phase1 v2.2)→ `e0c9c0a`(Cache-Control)→ `c40b7a7`(TaskF)→ `ebdbcdb`(テスト配置修正)
- 正常スナップショット ZIP: `C:\Users\user\Desktop\DeltaEngine_20260717_221316.zip`
- 保険フォルダ(削除禁止): `C:\Users\user\Desktop\DeltaEngine_broken_20260718`

---

# 2. 経緯(v3.14 からの差分のみ)

## Phase1_v2.2 — ライブバグ 4 件修正(2026-07-18)

| Task | 症状 | 修正 |
|------|------|------|
| A | IMBALANCE 毎バー strength=1.00 飽和 | min_volume null→E3002+0.5 / 分母ゼロは numerator≥min_volume 時のみ cap / strength 連続値化+3 バークールダウン |
| B | CVD「—」 | calibrate_cvd.py 新規、`cvd_slope_ref = 65.203`(7 日 P80、397 サンプル)書込。cvd_unref を仕様化 |
| C | CONFLUENCE 星ゼロ | WAIT 全 False 短絡撤廃、WAIT 中は composite 符号で方向判定 |
| D | 表示崩れ | #alerthist 通常フロー化 / 行ウィンドウ化 / [CLOSED]+経過秒 / Cache-Control: no-cache |
| E | signal.enabled 不整合 | `enabled: true` に統一 |

## TaskF — フットプリント順序契約違反(2026-07-18、新規発見・修正)

- **原因**: `footprint.to_levels()` は価格昇順契約、WebSocketPayload仕様_v1 §4.3 / `compute_value_area()` は降順前提。`webapp/main.py` アダプタが変換せず素通し。
- **症状**: UI 価格軸反転(安値が上)/ VAH・VAL 入替わり(VAL が POC の上に表示)/ 板 MID との見かけ 8 ドル乖離(現値帯が「▲ +N levels」に隠れる)。
- **修正**: アダプタ境界で `reversed(fp_bar.levels)`。delta 負ゼロ表示(|d|<0.05 → 符号なし 0.0)も修正。回帰テスト 2 本(payload 降順 + VAL<POC<VAH / reversed 配線ガード)。
- **教訓**: 既存 test 3 が降順入力でテストしていたためバグが素通りしていた。**契約境界のテストは実データの流入順で書く**。

## 体制の変化(重要)

- Claude Code が不調(システム不具合)のため、**TaskF 以降は web 側 Claude が直接実装**する体制に切替済み。web がサンドボックスで実装+pytest+配信検証まで完結し、修正ファイルを納品。お館様は配置・pytest 確認・commit・目視のみ。
- この体制は Code 復旧まで継続。復旧確認は `claude update` → `claude doctor`。

## ドキュメント整合(Phase 2、2026-07-18)

- Imbalance v3.1 → **v3.2**(§5.2 ゲート順序化 / §5.4 FlowEvent strength・クールダウン / §6 min_volume 0.5)
- SignalEngine v3.1 rev.(§4.1a cvd_unref / §8 追記 / §10 較正済み反映)
- CHANGELOG **v3.6.2** / CompletionLog 追記

---

# 3. 確定済み技術知見(再調査禁止)

- IMBALANCE スコア ±100 到達は仕様(stack_ref=3 で 3 スタック 1 本=±100)。頻度過多なら stack_ref を 5〜6 へ。
- cvd_slope_ref はサンプル 397 件と少なめ。データ蓄積後 `python tools\calibrate_cvd.py --write` で再較正推奨。
- フットプリントは確定バーのみ配信(ライブ進行中バーなし)。板との時間差は仕様、[CLOSED]+Ns で明示済み。
- git の実パスは `project/webapp/...`(リポジトリルートは DeltaEngine 直下、コードは project/ 配下)。
- CRLF 警告・改行コード差分は無害(Windows 環境由来)。

---

# 4. 運用ルール(厳守・v3.14 から更新)

- 指示書: 1 文書=1 フェーズ / PowerShell ネイティブ+絶対パス / コマンド改変禁止 / 途中承認なし・完了時のみ報告 / git diff による事後検証。
- `DeltaEngine` フォルダ自体の Rename/Remove 禁止(Code の cwd 制約)。復元は「ZIP を _zipref に展開 → robocopy /PURGE /IS /IT /XD data .git __pycache__」方式。
- 検証原則: 機械検証できることをユーザー目視に頼らない。目視は最終の見た目確認のみ。
- **web 直接実装時の完了条件**: pytest green + float( スキャン + 配信 HTML 機械検証 + 修正ファイル納品 + 配置手順の提示。
- web の応答規範: 結論ファースト・簡潔、判断が必要な分岐のみ提示、文末は「お館様」。

---

# 5. 次の課題(保留中・着手指示があるまで動かない)

1. ライブ進行中バーの配信(板との時間差の根本解消)
2. マルチシンボル(ETH 等: config 変更+min_volume 再較正でほぼ可能。symbol→streams 自動生成が望ましい)
3. 他取引所プロファイル(profiles/ 方式で拡張可)
4. MT5 経由 XAUUSD tick フロー実験
5. stack_ref・min_volume の実測較正
6. cvd_slope_ref 再較正(データ蓄積後)
7. Docker Desktop 正常起動確認(Windows コンポーネントストア破損対応の続き)/ `docker-compose up` での本番起動
