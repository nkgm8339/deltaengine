# 指示書: Phase 2-3 Stage 1 調査
Version: 1.0
発行: Claude(統括) → Codex(実装)
対象: DeltaEngine05M / Heatmap Phase 2-3(Canvas 2D 動的フレームレンダリング / recording駆動動的スクロール)
前提: Phase 2-2 Task 4 承認済(HEAD 869dc83c9556d74d32853ddcd061ce66bf01fa13)
段階: これは2段階フォーマットの Stage 1(調査)である。実装・変更・新規ファイル生成を一切行わない。読み取りと調査報告のみ。

---

## 0. 目的

Phase 2-3 の実装に着手する前に、既存資産・入力経路・技術的制約・保護ファイル接点・計測手法を実物ソースに基づいて棚卸しし、統括が実装方針(Stage 2)を確定できる材料を揃える。純Python PNG生成が実時間非対応であることは Task 4 で実証済(Full total_ms p95 = 21264.085690ms, 16ms参考予算超過)。Phase 2-3 はこの制約を前提に Canvas 2D 経路を評価する。

---

## 1. 絶対規律(調査中も遵守)

- 変更禁止: src/heatmap 配下、保護4ファイル(webapp/main.py, docker-compose.yml, tests/webapp/test_book_update.py, webapp/static/index.html)、git履歴、staging。読み取りのみ。
- 新規ファイル生成禁止(調査報告テキストの提出を除く)。
- 全ての主張・値・判断に実物の `ファイル:行番号` または仕様書該当節を併記せよ。根拠のない「確認済み」「一致」等の断定を禁ずる。
- 自己判定(GO/NO-GO/承認)を書くな。事実と行番号のみ提出せよ。判定は統括が行う。

---

## 2. 調査項目(I1〜I8。各項目、実物の ファイル:行番号 を必ず添えて回答せよ)

### I1. RenderReport.total_ms の定義確認(Task 4 承認時の指摘事項)
- `src/heatmap/render_static.py` の `RenderReport` 定義箇所を提示せよ。
- `total_ms` が何を計測しているか(transform+render の単純和か、PNG書き込みI/Oを含む実測総時間か)をソース行で示せ。
- 検証事実: Task 4 の last_report で total_ms が transform+render+aggregation を Full +44.1623ms / DS +8.4252ms 上回る。この差の発生源を該当行で説明せよ。

### I2. 既存 src/heatmap 資産の棚卸し
- `src/heatmap/` 配下の全モジュール(binner.py, reconstruct.py, render_static.py, transform.py, その他)について、公開クラス・関数のシグネチャを ファイル:行番号 付きで列挙せよ。
- Canvas 2D 経路で「再利用可能」「改修要」「不要」の分類は付けるな。事実(何が存在するか)のみ提出せよ。分類は統括が行う。

### I3. recording駆動動的スクロールの入力経路
- Task 4 で使用した `DepthHistoryReader` / `DepthReconstructor.sample_states` / `bin_samples` が、時系列サンプルをどの順序・粒度で供給するかをソース行で示せ。
- フレーム列(時間方向に1列ずつ進む描画)を生成する場合、既存の sample/grid 構造から1フレーム分を切り出す既存APIが存在するか。存在するなら ファイル:行番号、しないなら「該当なし」と明記せよ。

### I4. Canvas 2D 描画の配置とデータ送出経路
- 現行 webapp のサーバ→ブラウザのデータ送出経路(WebSocket等)の該当ソースを ファイル:行番号 で示せ。
- Canvas 2D 描画をブラウザ側で行う場合、サーバが送るべきデータ形状(グリッド/カラー値/差分)を既存構造から特定できるか、ソース根拠付きで報告せよ。
- webapp/static/index.html および webapp/main.py(いずれも保護ファイル)への接触が Phase 2-3 実装で不可避か否かを、現行の静的資産構成のソース根拠付きで報告せよ。接触が必要なら、その最小範囲を行番号で特定せよ(この段階では変更しない)。

### I5. 16ms フレームバジェット計測手法
- 静的計測(render_check.py)は40回総計573秒の集計だった。フレーム単位 p95 16ms を評価するための計測点(1フレームの transform→描画→提示)が既存コードのどこに置けるかを ファイル:行番号 で示せ。
- 既存に per-frame 計測の仕組みがあるか。なければ「該当なし」と明記せよ。

### I6. カラーマッピング確定値の踏襲確認
- 確定値: 色 = `(Decimal("1")+quantity).ln()` の p1-p99 クリップ、bid=青チャンネル `(0,0,i)`、ask=赤チャンネル `(i,0,0)`、レイヤー混合禁止、gap列=黒。
- 現行 render_static.py 内でこのマッピングを実装している箇所を ファイル:行番号 で示せ。Canvas経路でも同一値を再現するために参照すべき定数・関数を特定せよ。

### I7. ADR整合の確認点
- ADR-003(asyncio単一ループ、スレッドなし)、ADR-011(全量記録、事前フィルタ禁止)、Replay/Live同一applyコア共有。Phase 2-3 でこれらに抵触しうる設計上の論点を、現行コードのソース根拠付きで列挙せよ。抵触の有無の断定は不要。論点の提示のみ。

### I8. 保護ファイル接触の事前棚卸し
- I4 の結論を集約し、Phase 2-3 実装が保護4ファイルのいずれかに接触する見込みか否かを、ファイル単位で ○(接触見込み)/×(接触不要)/△(要統括判断)で提出せよ。理由に行番号を添えよ。

---

## 3. 提出物

- I1〜I8 の調査報告(テキスト)。各回答に実物の ファイル:行番号 を併記。
- 参照した全ファイルの HEAD 時点 SHA-256 と byte / LF / CR。
- 調査中に src/heatmap・保護ファイル・git・staging へ一切変更を加えていないことの証跡:
  - `git -C <root> rev-parse HEAD`(869dc83c... のままであること)
  - `git -C <root> status --porcelain`(空であること)

---

## 4. 禁止事項(再掲)

- 実装、リファクタ、新規ファイル生成、依存追加。
- GO/NO-GO/承認/推奨の記載。
- 「確認済み」「一致」等、行番号を伴わない断定。
- 出力の要約・整形。生の調査事実を提出せよ。

以上。承認後に Stage 2(実装)指示書を発行する。
