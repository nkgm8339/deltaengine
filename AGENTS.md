# DeltaEngine agent instructions

このリポジトリで作業するエージェントは、着手前に必ず
`ArchitectureRepository/00_Master/PROJECT_MEMORY.md` を全文読むこと。

同文書に記録されたプロジェクトの目的、2026-07-21 の再誕、完成した
Flow Price Response と3段チャートの意味を、変更判断の基準として扱うこと。

ユーザーから明示的な依頼がない限り、完成済みのフロー価格反応機能や3段チャートを
「改善」の名目で変更しないこと。ユーザーの最新の明示指示は常に優先する。

## 長時間・分割作業の継続規則

複数工程にまたがる実作業を開始した場合、承認待ち、失敗、時間制約、外部要因による
blockerは、該当操作とその直接依存工程だけを止める。現在の承認範囲内で安全かつ独立して
進められる調査、実装、試験、文書化を継続し、一部のblockerだけを理由に作業全体から
離脱してはならない。

作業固有の計画書またはpolicyがcheckpointを指定している場合、最初の変更前、各主要工程の
完了後、長時間処理の開始前後、承認要求前、未完了状態での終了前に逐次更新する。
checkpointには、少なくとも現在時刻、承認範囲、完了済み、未完了、変更file、検証結果、
blockerの限定範囲、次の再開位置を記録する。

未完了の実作業を、再開可能なcheckpointまたは引継ぎ記録なしで終了してはならない。
予期しない切断に備え、記録は最後にまとめて作るのではなく作業中に更新する。
