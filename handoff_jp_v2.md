# 次セッション引き継ぎドキュメント
## 分単位汎用タイムフレーム変換システム 日本語版

---

## 目次

1. 現在の状態
2. 次のゴール
3. 技術仕様（重要）
4. 実装方法
5. テスト計画
6. 8つの追加項目
7. 次セッション手順

---

## 1. 現在の状態

### 作成済み資料

```
✅ CVD_Footprint_SignalEngine_Complete_Guide.pdf
   - 32ページ
   - 基本理論と4パターン実行フロー

✅ TimeFrame_Conversion_Complete_Guide.pdf
   - 50ページ以上
   - 1M/3M/5M/10M の離散値換算表

✅ 本ドキュメント（次セッション向け）
```

### 実装済み機能

- CVD（累積出来高デルタ）計算
- Footprint チャート読み取り
- SignalEngine（複合判定）
- 4パターン（①②③④）

---

## 2. 次のゴール

### 現状の問題

```
❌ 2分足、7分足、15分足、30分足に対応していない
❌ 時間帯別のボラティリティ調整がない
❌ 複数通貨ペア対応がない
❌ エラーハンドリング不完全
```

### 達成目標

```
✅ 任意の分単位フレーム（0.5分～120分）に対応
✅ 自動計算で「誰でも簡単」
✅ 複数通貨・複数市場対応
✅ エラー対応完全
```

---

## 3. 技術仕様（重要）

### Master Formula（最核心）

#### ボラティリティ係数

```
元の公式：
  TP_nM = TP_1M × √n
  SL_nM = SL_1M × √n

例：
  1M: TP +100, SL -30
  5M: TP +100 × √5 = +224 pips
  7M: TP +100 × √7 = +265 pips
  10M: TP +100 × √10 = +316 pips
```

#### CVD 基準値

```
CVD は線形スケール

  CVD_基準値_nM = CVD_基準値_1M × n

例：
  1M: ±50
  5M: ±250
  10M: ±500
```

---

## 4. 実装方法

### Python テンプレート（簡易版）

```python
import math

class TimeFrameConverter:
    
    # 1M基準値
    BASE_PARAMS = {
        2: {'tp': 100, 'sl': 30, 'cvd': 50},  # パターン②
        3: {'tp': 250, 'sl': 75, 'cvd': 50},  # パターン③
        4: {'tp': 500, 'sl': 200, 'cvd': 50}  # パターン④
    }
    
    def convert(self, n, pattern):
        """n分足への変換"""
        if pattern not in self.BASE_PARAMS:
            raise ValueError("無効なパターン")
        
        base = self.BASE_PARAMS[pattern]
        vol_coef = math.sqrt(n)
        
        return {
            'tp': round(base['tp'] * vol_coef, 1),
            'sl': round(base['sl'] * vol_coef, 1),
            'cvd': round(base['cvd'] * n, 0)
        }

# 使用例
converter = TimeFrameConverter()
result = converter.convert(5, 2)
print(f"5分足パターン②: TP={result['tp']}, SL={result['sl']}")
```

### Excel テンプレート

```
入力セル：
  C2: n（分数）= 5
  C3: パターン = 2

計算式：
  E6 (TP): =100 * POWER(C2, 0.5)
  E7 (SL): =-30 * POWER(C2, 0.5)
  E9 (CVD): =50 * C2

出力：
  TP: 224 pips
  SL: -67 pips
  CVD基準値: 250
```

---

## 5. テスト計画

### ユニットテスト

```
□ n = 1 のとき、1M と同じ値か
□ n = 5 のとき、計算式が正しいか
□ 大きな n（100+）でエラーハンドリング
□ 小さな n（0.5）でエラーハンドリング
□ パターン①②③④全て動作確認
```

### バックテスト

```
過去3ヶ月データで検証：
  □ 5M と 7M で「どちらが勝率高いか」
  □ 期待値（Profit Factor）の計算
  □ ドローダウン測定
```

---

## 6. 8つの追加項目

### 項目①：時間帯別ボラティリティ調整

```
問題：同じ 5M でも時間帯で値動きが異なる

調整例：
  東京朝（08-10時）：ボラ 60%（薄い）
  ロンドン（16-24時）：ボラ 100%（標準）
  ニューヨーク（21-06時）：ボラ 130%（厚い）

公式：
  TP_adjusted = TP_nM × 時間帯係数
  
  例：5M東京朝
    TP = 224 × 0.60 = 134 pips
```

### 項目②：複数通貨ペア対応

```
問題：BTC と ETH でボラが大きく異なる

調整表：
  BTCUSD: 係数 1.0（基準）
  ETHUSD: 係数 0.35（ボラ小さい）
  SOLUSD: 係数 1.5（ボラ大きい）

公式：
  TP_asset = TP_nM × 通貨別係数
  
  例：5分足ETH
    TP = 224 × 0.35 = 78 pips
```

### 項目③：エラーハンドリング

```
対応必須ケース：
  □ n < 1（30秒足）→ パターン①のみ
  □ n > 120（2時間超）→ エラー
  □ 市場閉場時 → 取引量チェック
  □ 突発ボラ上昇 → ATR で動的調整
  □ データ不足 → デフォルト値使用
```

### 項目④：A/B テスト

```
目的：「7M は 5M より優れているか？」を統計的検証

実施方法：
  過去3ヶ月で両方を実装
  勝率、損益比、Sharpe Ratio を比較
  p値 < 0.05 なら「有意差あり」
```

### 項目⑤：動的最適化

```
従来：係数 = √n（固定）

改善：
  過去100トレード分析
  実際の TP 到達率を測定
  理論値と乖離していたら係数を調整
  3ヶ月ごとに更新
```

### 項目⑥：パターン遷移

```
例：5M パターン② で 15分保有予定
    → 15分経過してもTP未達成

対応：
  残りポジション 50% をクローズ
  残り 50% をパターン③に移行
  TP/SL を再計算
```

### 項目⑦：ダッシュボード

```
入力：n, パターン, 通貨, 時間帯
  ↓
出力表示：
  TP/SL/CVD基準値
  過去3ヶ月の成績グラフ
  勝率/Sharpe Ratio
  推奨度（★★★など）
  警告メッセージ
```

### 項目⑧：クロスバリデーション

```
問題：パラメータが「Binance Futures」で有効でも
     「MT5」では無効かもしれない

対応：
  複数市場で同じパラメータをテスト
  市場別に微調整係数を設定
  「全市場で有効」なパラメータを採用
```

---

## 7. 次セッション手順

### Phase 1（1時間）：コア実装

```
□ timeframe_converter.py 作成
□ convert() 関数実装
□ ユニットテスト 20個
□ 全テスト Pass 確認
```

### Phase 2（2時間）：拡張機能

```
□ 時間帯別調整コード追加（項目①）
□ 通貨別調整コード追加（項目②）
□ エラーハンドリング実装（項目③）
□ 統合テスト実施
```

### Phase 3（2時間）：高度な機能

```
□ A/B テストフレームワーク（項目④）
□ 動的最適化ロジック（項目⑤）
□ パターン遷移処理（項目⑥）
□ バックテスト実施
```

### Phase 4（1-2時間）：仕上げ

```
□ ダッシュボード UI 作成（項目⑦）
□ クロスバリデーション実装（項目⑧）
□ 最終ドキュメント作成
□ 本番環境テスト
```

### 合計時間：6～7時間

---

## チェックリスト

```
開始前確認：
  □ Python 3.8+ インストール済み
  □ pandas, numpy インストール済み
  □ /mnt/user-data/outputs/ にアクセス可能
  □ このドキュメント読了済み

実装完了後：
  □ pytest で 50+ テスト成功
  □ バックテスト結果が期待値を超えている
  □ ドキュメント完成
  □ UI が動作している
```

---

## 推奨パラメータ（参考値）

### パターン②（短期スキャル）

```
タイムフレーム  TP基本  TP(5時間)  SL基本  SL(5時間)
1M            +100    +100       -30    -30
3M            +150    +150       -45    -45
5M            +200    +200       -60    -60
7M            +265    +265       -80    -80
10M           +316    +316       -95    -95
```

### パターン③（中期スイング）

```
タイムフレーム  TP基本  SL基本
1M            +250    -75
5M            +559    -168
10M           +791    -237
```

---

## 質問対応（予想Q&A）

### Q1: 時間帯係数は固定か、動的か？

```
A: 最初は固定テーブル。
   過去データから動的に最適化できるが、初期は固定推奨。
```

### Q2: 通貨ペア係数は手動入力か？

```
A: 手動で設定可能。
   将来的には ATR から自動計算可能。
```

### Q3: すべてのパターンに対応？

```
A: パターン①（ウルトラスキャル）は 1M のみ。
   パターン②③④は全フレーム対応。
```

### Q4: バックテスト期間は？

```
A: 最小 3ヶ月。
   100+ トレードを推奨。
```

---

## 次セッション開始時にやること

```
1. このドキュメントを読む（必須）
2. Master Formula の理解確認
3. Python テンプレートをコピペして動作確認
4. Phase 1 から開始
5. 不明点は「このドキュメント」に戻って確認
```

---

## ファイル場所

```
参考資料：
  /mnt/user-data/outputs/
    ├─ CVD_Footprint_SignalEngine_Complete_Guide.pdf
    ├─ TimeFrame_Conversion_Complete_Guide.pdf
    └─ このファイル（Markdown + PDF版）

実装対象：
  /mnt/user-data/outputs/
    └─ timeframe_converter.py（次セッションで作成）
```

---

## 最後に

このシステムが完成すると、ユーザーは：

```
「5分足」と入力
  ↓
「全パラメータ自動計算」
  ↓
「複数市場・複数通貨対応」
  ↓
「統計的に検証済み」
  ↓
「即座にトレード実装」
```

ができるようになります。

次セッション、頑張ります。

---

ハンドオフドキュメント完成版
作成日：2026年7月19日
バージョン：2.0（日本語版）
対象：DeltaEngine 次セッション実装
