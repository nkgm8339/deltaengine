"""Data acquisition — bounded queues, WebSocket connector, DataReceiver (M4).

See event_queue (ADR-002 bounded queue), connector (WebSocket lifecycle +
reconnect), receiver (validate/record/forward), replay (JSON Lines replay).
"""
